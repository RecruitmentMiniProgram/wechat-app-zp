<view class="chat">
  <view wx:if="{{type != 0}}">
        <scroll-view bindtouchstart="scrollstart" scroll-y scroll-into-view='{{toView}}' style='height: {{scrollHeight}};'>

        <view wx:for="{{msgList}}" wx:key="index" class="news {{item.role == 1? 'contrary' : ''}}" id="msg-{{index}}">
          <view class = "news-centent {{item.role == 1?'centent-right' : 'centent-left'}}">

            <view class="{{item.role == 1?'trianright':'trianleft'}}"></view>

            <text selectable="{{true}}" class="{{item.role == 1?'background_right':'background_left'}}" >{{item.msg}}</text>

          </view>
        </view>
      </scroll-view>

        <!-- 底部功能栏 -->
        <view class='inputRoom' style='bottom: {{inputBottom}}'>
          <input bindconfirm='sendClick' adjust-position='{{false}}' value='{{inputVal}}' confirm-type='send' bindfocus='focus' bindblur='blur' type="text"></input>
      </view>
</view>

<view wx:else="">
  <scroll-view scroll-y="{{true}}">
    <view class="mylist-item" wx:for="{{postList}}" wx:key="index">
      <text class="mytime">{{item.time}}</text>
      <button style="width: 90%; height: 130px;" class="mybutton" bindtap="onButtonClick" data-index="{{index}}">{{item.name}}</button>
    </view>
</scroll-view>
</view>

</view>
